<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.twlkyao.androidcloud"
    android:versionCode="2"
    android:versionName="1.1" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="14" />
    <!-- The permission to access the Internet. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- The permission to read and write file. -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- The permission to create, move, delete file and directory. -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="com.twlkyao.androidcloud.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.twlkyao.androidcloud.RegisterActivity" >
        </activity>
        <activity android:name="com.twlkyao.androidcloud.WelcomeActivity" >
        </activity>
        <activity android:name="com.twlkyao.androidcloud.MainActivity" >
        </activity>
    </application>

</manifest>