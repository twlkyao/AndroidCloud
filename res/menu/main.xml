<menu xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- The set base key menu. -->
    <item
        android:id="@+id/action_set_base_keys"
        android:orderInCategory="100"
        android:title="@string/action_set_base_keys"/>
    <!-- The show contacts menu. -->
    <item
        android:id="@+id/action_show_contacts"
        android:orderInCategory="200"
        android:title="@string/action_show_contacts"/>
    <!-- The show messages menu. -->
    <item
        android:id="@+id/action_show_messages"
        android:orderInCategory="300"
        android:title="@string/action_show_messages"/>
    <!-- The help menu. -->
    <item
        android:id="@+id/action_help"
        android:orderInCategory="400"
        android:title="@string/action_help"/>

</menu>